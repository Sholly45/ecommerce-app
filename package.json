{
  "name": "ecommerce-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "webpack --mode production",  // Add or verify this line
    "test": "jest"  // Ensure you have this or similar for your tests
  },
  "dependencies": {
    "express": "^4.17.1",
    // other dependencies
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0",
    // other dev dependencies
  }
}

